apiVersion: 1

title: My Grafana Dashboard
tags: [sample, test]

panels:
  - type: graph
    title: Panel 1
    dataSource: Prometheus
    targets:
      - target: "metric1"
        refId: A

  - type: singlestat
    title: Panel 2
    dataSource: Prometheus
    targets:
      - target: "metric2"
        refId: B